{
    "name": "socket-share",
    "version": "1.0.0",
    "private": true,
    "main": "electron/main.js",
    "workspaces": [
        "client",
        "server",
        "electron"
    ],
    "scripts": {
        "dev": "concurrently -n server,client,electron -c cyan,green,magenta \"npm:dev -w server\" \"npm:dev -w client\" \"cross-env SKIP_SERVER=true npm:dev -w electron\"",
        "build": "npm run build -w client --if-present",
        "lint": "npm run lint -w server && npm run lint -w client",
        "start": "npm run start -w electron"
    },
    "devDependencies": {
        "concurrently": "^9.0.1",
        "wait-on": "^8.0.1"
    },
    "description": "P2P LAN File Sharing"
}
